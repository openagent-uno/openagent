# Standalone client transport

App/CLI/server protocol compatibility in one product package. Identity delegates to openagent-identity; event contracts delegate to Core. No generated implementation copies.
